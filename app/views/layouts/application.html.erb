<!DOCTYPE html>
<html>
  <head>
    <title>UW Computer Science Society</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

    <link rel="shortcut icon" type="image/png" href="http://www.uwindsor.ca/sites/all/themes/uwindsor_bootstrap/favicon-32x32.png">
  </head>

  <body>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <%= image_tag("uwindsor_logo.png", size: "38x45", style:"margin-right:10px;") %>
        <a class="navbar-brand" href=""
        <%= link_to root_url, class: "navbar-brand" do %>
          Computer Science Society
        <% end %>

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav mr-auto">
            <li class="nav-item <%= "active" if current_page?(root_path) %>">
              <%= link_to "Home", root_url, class: "nav-link" %>
            </li>
            <li class="nav-item <%= "active" if current_page?(events_path) %>">
              <%= link_to "Events", events_path, class: "nav-link" %>
            </li>
            <li class="nav-item <%= "active" if current_page?(:discord_path) %>">
              <%= link_to "Discord", :discord_path, class: "nav-link" %>
            </li>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle <%= "active" if current_page?(:resources) %>" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Resources
              </a>
              <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                <a class="dropdown-item" href="http://tutor.cs.uwindsor.ca/" target="_blank">Tutoring Schedule</a>
                <div class="dropdown-divider"></div>
                <%= link_to "Student Guide", :resources, class: "dropdown-item #{current_page?(:resources) ? 'active' : ''}" %>
              </div>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="https://github.com/EricPickup/uwindsor-css-events-site" target="_blank">Contribute</a>
            </li>
            <li class="nav-item">
              <a class="nav-link disabled" href="#">Problem of the Week</a>
            </li>
          </ul>
          <form class="form-inline my-2 my-lg-0">
            <% if current_user %>
              <div style="color: white;margin-right:10px;">Welcome, <%= current_user.name %>!</div>
              <%= link_to "Sign out", :logout, class: "btn btn-primary" %>
            <% else %>
              <%= link_to "Sign in", :google_auth, class: "btn btn-primary" %>
            <% end %>
          </form>
        </div>
      </nav>


    <% if flash[:error] %>
      <p><%= flash[:error] %></p>
    <% end %>
    <% if flash[:success] %>
      <p><%= flash[:success] %></p>
    <% end %>

    <%= yield %>
  </body>
</html>
