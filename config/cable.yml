development:
  adapter: async

test:
  adapter: async

production:
  adapter: redis
  url: redis://localhost:6379/1
  url: <%= ENV.fetch('ACTION_CABLE_REDIS_URL', 'redis://localhost:6379/1') %>
  channel_prefix: uwindsor-css-events-site_production
